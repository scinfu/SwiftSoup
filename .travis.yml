os:
  - linux
  - osx
 # safelist
branches:
  only:
  - master
language: generic
sudo: required
dist: trusty
osx_image: xcode8.1
script:
  - eval "$(curl -sL https://raw.githubusercontent.com/brokenhandsio/SwiftSoup/master/ci)"
